---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: jira-service
  namespace: keptn
spec:
  selector:
    matchLabels:
      run: jira-service
  replicas: 1
  template:
    metadata:
      labels:
        run: jira-service
    spec:
      containers:
      - name: jira-service
        image: adamgardnerdt/keptn-jira-service:v1
        imagePullPolicy: Always
        ports:
        - containerPort: 80
        env:
        - name: JIRA_URL
          valueFrom:
            secretKeyRef:
              name: jira-details
              key: url
        - name: JIRA_USERNAME
          valueFrom:
            secretKeyRef:
              name: jira-details
              key: username
        - name: JIRA_API_TOKEN
          valueFrom:
            secretKeyRef:
              name: jira-details
              key: api-token
        - name: JIRA_PROJECT_KEY
          valueFrom:
            secretKeyRef:
              name: jira-details
              key: project-key
        - name: JIRA_ISSUE_TYPE
          valueFrom:
            secretKeyRef:
              name: jira-details
              key: issue-type
---
apiVersion: v1
kind: Service
metadata:
  name: jira-service
  namespace: keptn
  labels:
    run: jira-service
spec:
  ports:
  - port: 80
    protocol: TCP
  selector:
    run: jira-service